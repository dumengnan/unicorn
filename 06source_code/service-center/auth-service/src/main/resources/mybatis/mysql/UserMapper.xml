<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.piggymetrics.auth.repository.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.piggymetrics.auth.domain.User">
        <result column="USERNAME" property="username"/>
        <result column="PASSWORD" property="password"/>
    </resultMap>

    <select id="findUser" resultMap="userResultMap" parameterType="java.lang.String">
        select * from user where username=#{userName}
    </select>

    <insert id="createUser" parameterType="com.piggymetrics.auth.domain.User">
        insert into user(username, password) values(#{username}, #{password})
    </insert>

</mapper>